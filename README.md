# Flux Router Engine

A high-performance, scalable routing engine for large-scale network infrastructures.

## Getting Started
1. Initialize the project: `go mod init flux-router-engine`
2. Run the command: `go build src/main.go` then `./main`